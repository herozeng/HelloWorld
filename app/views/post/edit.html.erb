<h1>Post#edit</h1>



<%= form_for @post, url: {action: "update"} do |f| %>
  <p>
	<%=f.label :title %><br>
	<%= f.text_field :title %>
  </p>
  <p>
	<%=f.label :content %><br>
	<%= f.text_area :content %>
  </p>
  <p>
	<%= f.submit "Update a post" %>
  </p>
<% end %>

